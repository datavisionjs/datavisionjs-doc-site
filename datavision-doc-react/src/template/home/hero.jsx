

const Hero = () => {
  
    return (
        <div className="hero">
            <h1>DATAVISION.JS</h1>
            <p>
                A robust charting library with built in support for common aggregation functions (sum, avg, min, max, count) and interactive charts (bar, line, pie & more).
            </p>
        </div>
    );
};
    
export default Hero;